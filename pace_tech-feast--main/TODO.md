# TODO

- [x] Generate `requirements.txt` from `pyproject.toml`
- [x] Install dependencies (pip)
- [x] Run `python app.py` and capture errors

- [ ] Fix any import/runtime errors found
- [ ] Re-run `python app.py` until it starts successfully
- [ ] Verify key endpoints: `/api/domains`, `/api/news`, `/api/generate-pdf`, `/api/latest-pdf`

